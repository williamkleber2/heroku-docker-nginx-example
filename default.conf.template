upstream frontend {
    server $FRONTEND_URL;
}

upstream backend {
    server $BACKEND_URL;
}

server {
  listen $PORT default_server;

  
  location /about {
        proxy_pass frontend;
    }

  location / {
        proxy_pass backend;
    }
}
