build:
  docker:
    web: Dockerfile

release:
  command:
    - chmod u+x release-tasks.sh  && ./release-tasks.sh
  image: web

run:
  web: newrelic-admin run-program gunicorn project.wsgi --log-file -